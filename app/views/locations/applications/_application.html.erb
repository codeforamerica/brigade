<tr class="<%= cycle "odd", "even" %>", id="<%= "application_#{application.id}" %>">
  <td><%= application.name %></td>
  <td><%= link_to 'Start!', application_path(application), class: "show-button"  %></td>
</tr>
